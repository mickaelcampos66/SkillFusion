name: Pull Request

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  app-frontend:
    uses: ./.github/workflows/app-frontend.yml
  app-backend:
    uses: ./.github/workflows/app-backend.yml
